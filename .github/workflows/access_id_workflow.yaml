name: secrets

on: 
  push: 
   branches:
   - main

jobs:
  print-message:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run : echo "The ACCESS_ID is " ${{ vars.ACCESS_ID }}
      
